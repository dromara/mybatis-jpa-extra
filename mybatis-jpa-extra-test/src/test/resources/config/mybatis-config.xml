<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE configuration
  PUBLIC "-//mybatis.org//DTD Config 3.0//EN"
  "https://mybatis.org/dtd/mybatis-3-config.dtd">
<configuration>
	<properties resource="config/applicationConfig.properties"></properties>
	<typeAliases>
		<package name="org.dromara.mybatis.jpa.test.entity"/>
	</typeAliases>
	
	<environments default="development">
		<environment id="development">
			<transactionManager type="JDBC" />
			<dataSource type="POOLED">
				<property name="driver"
					value="${config.datasource.driverclass}" />
				<property name="url" value="${config.datasource.url}" />
				<property name="username"
					value="${config.datasource.username}" />
				<property name="password"
					value="${config.datasource.password}" />
			</dataSource>
		</environment>
	</environments>
	 
	

	<mappers>
		<mapper
			resource="org/dromara/mybatis/jpa/test/dao/persistence/xml/mysql/StudentsMapper.xml" />
	</mappers>
</configuration>